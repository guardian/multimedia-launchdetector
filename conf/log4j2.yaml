Configuration:
  Appenders:
    Lambda:
      name: Lambda
      PatternLayout:
        pattern: "%d{yyyy-MM-dd HH:mm:ss} %X{AWSRequestId} %-5p %c{1}:%L - %m%n"

  Loggers:
    Root:
      level: debug
      AppenderRef:
        ref: Lambda
